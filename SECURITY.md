# Security

Please report vulnerabilities privately by emailing `security@example.com`.

Do not include live API keys, operator tokens, provider passwords, webhook secrets, or message bodies in public issues. Agent Mailbox redacts known token formats in logs and keeps data local by default.
